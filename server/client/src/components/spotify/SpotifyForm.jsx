import React, { Component } from "react";
import { reduxForm } from "redux-form";

class SpotifyForm extends Component {
  render() {
    return <div />;
  }
}

export default reduxForm()(SpotifyForm);
